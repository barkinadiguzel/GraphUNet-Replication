# GraphUNet-Replication
GraphUNet-Replication is a PyTorch implementation of the Graph U-Nets architecture, reproducing the gPool and gUnpool layers for forward-pass experiments. It focuses on encoding and decoding graph-structured data without training, following the original paper’s block diagrams and mathematical formulations.
